# attiny-uploader CLI

Python host tool for the ESP32 ATtiny UPDI programmer. See the [project README](../README.md) for wiring, usage, and build-system integration.

## Development

```bash
cd cli
python -m pip install -e ".[dev]"
pytest
attiny-uploader --help
```

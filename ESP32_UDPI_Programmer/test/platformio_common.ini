; Shared USB serial settings for all test sketches (GPIO 1/3 to PC).
; monitor_speed and USB_SERIAL_BAUD must stay in sync.

[common]
monitor_speed = 115200
monitor_dtr = 0
monitor_rts = 0
usb_serial_baud = -DUSB_SERIAL_BAUD=115200
